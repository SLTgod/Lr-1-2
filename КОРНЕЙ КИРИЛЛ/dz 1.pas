var x,y,a:real;
begin
  readln(x);
  if x < -7 then;
  y:= (x*x) / ln(x) +(x*x);
 if (-7 <= x) and (x < -1) then
    y:= tan(x) -68;
   if -1 <= x then
    a:= exp(0.1*x);
  y:= 18/44 + sin(x);
  writeln(y);
end.